# CPP Schedule Builder


This project uses a Python helper script to retrieve professor ratings.
Source: https://pypi.org/project/RateMyProfessorAPI/ 

Install the required Python package with:

```bash
python -m pip install -r requirements.txt
